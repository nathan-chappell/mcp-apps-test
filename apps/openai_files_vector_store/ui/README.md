# UI Placeholder

This directory is intentionally reserved for the later MCP App UI phase.

The backend is implemented first so the VS Code agent can already use and validate the OpenAI Files + Vector Store workflow. When we switch to the UI/operator-console phase, this directory will hold the inline MCP App resources and frontend build output.
